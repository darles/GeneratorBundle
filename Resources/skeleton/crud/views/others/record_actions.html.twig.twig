    <a href="{{ "{{ path('" ~ route_name_prefix ~ "') }}" }}" class="btn">
        <i class="icon-chevron-left"></i> 
        {{ "{{ 'Back to the list'|trans }}" }}
    </a>
{% if ('edit' in actions) and (not hide_edit) %}
    <a href="{{ "{{ path('" ~ route_name_prefix ~ "_edit', { 'id': entity.id }) }}" }}" class="btn">
        <i class="icon-pencil"></i> 
        {{ "{{ 'Edit'|trans }}" }}
    </a>
{% endif %}
{% if ('delete' in actions) and (not hide_delete) %}
    <a href="{{ "{{ path('" ~ route_name_prefix ~ "_delete', { 'id': entity.id }) }}" }}" class="btn" onclick="return confirm('{{ "{{ 'Confirm delete'|trans }}" }}');">
        <i class="icon-trash"></i>
        {{ "{{ 'Delete'|trans }}" }}
    </a>
{% endif %}