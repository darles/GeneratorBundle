{{ "<? $view->extend('::base.html.php'); ?>" }}

{{ "<?php $view['slots']->start('header_row') ?>" }}
    <div class="span5">
        <h2>{{ "<?=$view['translator']->trans('" ~ entity ~ "')?>" }}</h2>
    </div>
    <nav class="span4">
        {% set hide_edit, hide_delete = false, false %}
        {% include 'views/others/record_actions.html.php.twig' %}
    </nav>
{{ "<?php $view['slots']->stop('header_row') ?>" }}

<div class="box">
    <div class="row-fluid">
        <div class="content">
            <table>
                <tbody>
                    {%- for field, metadata in fields %}

                    <tr>
                        <th>{{ field|capitalize }}</th>

                    {%- if metadata.type in ['date', 'datetime'] %}

                        <td>{{ '<?=(string)$entity->get' ~ field|replace({'_': ''}) ~ '()?>' }}</td>

                    {%- else %}

                        <td>{{ '<?=$entity->get' ~ field|replace({'_': ''}) ~ '()?>' }}</td>

                    {%- endif %}

                    </tr>

                {%- endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>