{{ "<? $view->extend('::base.html.php'); ?>" }}

{{ "<? $view['slots']->start('breadcrumb') ?>" }}
    <li>
        <a class="crumb active" href="{{ "<?= $view['router']->generate('" ~ route_name_prefix ~ "') ?>" }}">
            <?=$view['translator']->trans('{{ translation_prefix }}_name')?>
        </a>
    </li>
{{ "<? $view['slots']->stop('breadcrumb') ?>" }}

{{ "<? $view['slots']->start('box_header') ?>" }}
    <i class="icon-th-list"></i>
    <?=$view['translator']->trans('{{ translation_prefix }}_name')?>
{{ "<? $view['slots']->stop('box_header') ?>" }}

{{ "<? $view['slots']->start('box_buttons') ?>" }}
    {% if 'new' in actions %}
    <a href="{{ "<?= $view['router']->generate('" ~ route_name_prefix ~ "_new') ?>" }}" class="btn btn-mini">
        <i class="icon-file"></i> <?=$view['translator']->trans('{{ translation_global_prefix }}create_new')?>
    </a>
    {% endif %}
{{ "<? $view['slots']->stop('box_buttons') ?>" }}

{{ "<?=$view->render('" ~ bundle ~ ":" ~ entity ~ ":_filter.html.php', array('form' => $searchForm))?>" }}

<table
    class="table table-bordered table-striped data-table"
    data-bind="dataTable"
    data-source="{{ "<?= $view['router']->generate('" ~ route_name_prefix ~ "_getdata') ?>" }}"
    data-actions-prototype="{{ "<?=$view->escape($view->render('" ~ bundle ~ ":" ~ entity ~ ":_actions.html.php')) ?>" }}"
>
    <thead>
        <tr>
            <td><?=$view['translator']->trans('global.id')?></td>
            <td><?=$view['translator']->trans('global.name')?></td>
            <td><?=$view['translator']->trans('global.actions')?></td>
        </tr>
    </thead>
</table>