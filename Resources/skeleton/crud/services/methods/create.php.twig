
    /**
     * Persist newly created entity 
     *
     * @param {{ entity }} $entity
     * @return bool
     */
    public function create({{ entity }} $entity)
    {
        $this->em->persist($entity);

        return true;
    }